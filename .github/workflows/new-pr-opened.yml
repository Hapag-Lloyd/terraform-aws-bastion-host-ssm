---
name: PR opened

on:
  pull_request_target:
    # GITHUB_TOKEN is readonly and the action will fail for Dependabot
    branches-ignore:
      - 'dependabot/**'
    types:
      - opened

jobs:
  default:
    uses: Hapag-Lloyd/Workflow-Templates/.github/workflows/new-pr-opened.yml@40a65158591c1c4b7526d095f2ed58e1c512941f # 
    secrets: inherit
