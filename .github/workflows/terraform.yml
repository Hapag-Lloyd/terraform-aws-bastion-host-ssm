---
name: Terraform

# yamllint disable-line rule:truthy
on:
  pull_request:

permissions:
  id-token: write
  contents: read

jobs:
  default:
    # yamllint disable-line rule:line-length
    uses: Hapag-Lloyd/Workflow-Templates/.github/workflows/terraform_module_terraform_callable.yml@41f9104fbc6b2c40a688812239215907bedafebe # 1.22.32
    secrets: inherit
    with:
      minimum-terraform-version: "1.5.7"
